import 'package:flutter/material.dart';

class ArtistBookingsPage extends StatelessWidget {
  const ArtistBookingsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Artist Bookings page'),
      ),
    );
  }
}