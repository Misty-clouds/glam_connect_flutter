import 'package:flutter/material.dart';

class ArtistMessagesPage extends StatelessWidget {
  const ArtistMessagesPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Artist Messages page'),
      ),
    );
  }
}