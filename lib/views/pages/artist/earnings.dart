import 'package:flutter/material.dart';

class ArtistEarningsPage extends StatelessWidget {
  const ArtistEarningsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Artist Earnings page'),
      ),
    );
  }
}