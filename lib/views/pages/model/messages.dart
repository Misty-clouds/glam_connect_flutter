import 'package:flutter/material.dart';

class ModelMessagesPage extends StatelessWidget {
  const ModelMessagesPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Model Messages Page'),
      ),
    );
  }
}
