import 'package:flutter/material.dart';

class ModelProfilePage extends StatelessWidget {
  const ModelProfilePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Model Profile Page'),
      ),
    );
  }
}
