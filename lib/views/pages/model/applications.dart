import 'package:flutter/material.dart';

class ModelApplicationsPage extends StatelessWidget {
  const ModelApplicationsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Model Applications Page'),
      ),
    );
  }
}
