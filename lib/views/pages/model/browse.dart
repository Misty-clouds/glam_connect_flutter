import 'package:flutter/material.dart';

class ModelBrowsePage extends StatelessWidget {
  const ModelBrowsePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Model Browse Page'),
      ),
    );
  }
}
