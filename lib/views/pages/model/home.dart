import 'package:flutter/material.dart';

class ModelHomePage extends StatelessWidget {
  const ModelHomePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Model Home Page'),
      ),
    );
  }
}
